# Port publie sur Windows. L'application reste sur le port 3000 dans le conteneur.
APP_PORT=3000

# MySQL/MariaDB fourni par WAMP sur la machine Windows.
DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=beer_app
DB_PASSWORD=beer_pass
DB_NAME=beer
